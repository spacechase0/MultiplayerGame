#ifndef PACKET_HPP
#define PACKET_HPP

namespace net
{
    constexpr int VERSION =
    class Packet
    {
        public:
            enum Id
            {
                Version,
            };
    };
}

#endif // PACKET_HPP
